name: scrabble-score

dependencies:
  - base

library:
  exposed-modules: Scrabble
  source-dirs: src
  dependencies:
    containers >= 0.5
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - scrabble-score
      - hspec
